# Agent Concierge — environment
#
# Mock mode (default) runs the scripted demo with no network calls — no key needed.
# Live mode reads the key below and calls Gemini 3 Flash directly from the browser.
# Exposing a key in a browser build is a prototype-only pattern. For production,
# front the model through your own backend or Firebase AI Logic.

VITE_GEMINI_API_KEY=
